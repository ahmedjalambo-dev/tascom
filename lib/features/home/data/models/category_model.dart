/// Model representing a category for filtering tasks.
class CategoryModel {
  final String id;
  final String name;
  final String iconPath;

  const CategoryModel({
    required this.id,
    required this.name,
    required this.iconPath,
  });
}
